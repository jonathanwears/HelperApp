import { StyleSheet } from 'react-native';

export const CounterButtonStyle = StyleSheet.create({
  container: {
    borderRadius: 5,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 0,
    margin: 0,
    height: 50,
    width: 100,
  },
});

export const ButtonColors = {
  pressed: '#d2e6ff',
  released: '#82b1ed',
};
